const pages=document.querySelectorAll('.page'),nav=document.querySelectorAll('nav button');function show(id){pages.forEach(p=>p.classList.toggle('active',p.id===id));nav.forEach(b=>b.classList.toggle('active',b.dataset.page===id));scrollTo(0,0)}nav.forEach(b=>b.onclick=()=>show(b.dataset.page));document.querySelectorAll('[data-goto]').forEach(b=>b.onclick=()=>show(b.dataset.goto));const alumni=[{n:'Iskandar M. Tjut',a:'—',p:'Jurnalis',d:'Lhokseumawe'}];function render(){const q=(document.getElementById('search')?.value||'').toLowerCase(),box=document.getElementById('alumniList');if(!box)return;box.innerHTML=alumni.filter(x=>(x.n+' '+x.a+' '+x.p+' '+x.d).toLowerCase().includes(q)).map(x=>`<div class="card"><b>👤 ${x.n}</b><p>Angkatan: ${x.a}<br>Profesi: ${x.p}<br>📍 ${x.d}</p></div>`).join('')}document.getElementById('search')?.addEventListener('input',render);render();if('serviceWorker'in navigator)navigator.serviceWorker.register('service-worker.js');
