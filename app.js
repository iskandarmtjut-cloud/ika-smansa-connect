const client=window.supabase.createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY);let rows=[];const $=s=>document.querySelector(s);function go(id){document.querySelectorAll(".page").forEach(x=>x.classList.toggle("on",x.id===id));document.querySelectorAll("nav button").forEach(x=>x.classList.toggle("on",x.dataset.page===id));if(id==="alumni")loadAlumni()}document.querySelectorAll("nav button").forEach(x=>x.onclick=()=>go(x.dataset.page));function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]))}async function loadAlumni(){status.textContent="Mengambil data online...";const {data,error}=await client.from("alumni").select("id,nama,angkatan,tahun_lulus,profesi,domisili").order("nama");if(error){status.textContent="Database belum tersambung.";alist.innerHTML='<div class="card">⚠️ Periksa config.js dan policy Supabase.</div>';return}rows=data||[];status.textContent=rows.length+" alumni tersimpan online.";render()}function render(){const q=(search.value||"").toLowerCase();const a=rows.filter(x=>Object.values(x).join(" ").toLowerCase().includes(q));ca.textContent=rows.length;alist.innerHTML=a.length?a.map(x=>`<div class="card"><h3>👤 ${esc(x.nama)}</h3><p>🎓 Angkatan: ${esc(x.angkatan||"-")}<br>📅 Lulus: ${esc(x.tahun_lulus||"-")}<br>💼 ${esc(x.profesi||"-")}<br>📍 ${esc(x.domisili||"-")}</p></div>`).join(""):'<div class="card">Belum ada alumni yang cocok.</div>'}function openForm(){modal.classList.remove("hide")}function closeForm(){modal.classList.add("hide")}search.oninput=render;form.onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form));const {error}=await client.from("alumni").insert(d);if(error){alert("Gagal menyimpan. Periksa policy INSERT Supabase.");return}alert("Profil berhasil disimpan online.");form.reset();closeForm();loadAlumni()};loadAlumni();